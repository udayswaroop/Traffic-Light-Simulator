// ResetTimerButton.js

const ResetTimerButton = ({ onClick }) => {
  return <button style={{backgroundColor: 'blue', color: 'white'}} onClick={onClick}>Reset Timer</button>;
};

export default ResetTimerButton;

