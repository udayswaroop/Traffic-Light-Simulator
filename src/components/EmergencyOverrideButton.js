// EmergencyOverrideButton.js

const EmergencyOverrideButton = ({ onClick }) => {
  return <button style={{backgroundColor: 'red', color: 'white'}} onClick={onClick}>Emergency Override</button>;
};

export default EmergencyOverrideButton;
